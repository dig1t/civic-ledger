# CivicLedger Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# Database Configuration (optional - defaults in application-dev.properties)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=civicledger
# DB_USERNAME=admin
# DB_PASSWORD=securepassword123!

# JWT Configuration (optional - has dev defaults)
# JWT_SECRET=your-secure-jwt-secret-key-here

# Encryption Master Key (required for persistent document access)
# Generate with: openssl rand -base64 32
# Without this, documents become inaccessible after server restart
ENCRYPTION_MASTER_KEY=your-base64-encryption-key-here

# OpenAI API Configuration (required for AI summaries)
OPENAI_API_KEY=sk-proj-your-api-key-here
